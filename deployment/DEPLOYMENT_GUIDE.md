# Полный гайд по развёртыванию Japan Travel Map

## Этап 1: Подготовка проекта и загрузка на GitHub

### 1.1 Установка Git

**Windows:**
1. Скачайте Git с официального сайта: https://git-scm.com/download/win
2. Запустите установщик и нажимайте "Next" (настройки по умолчанию подойдут)
3. После установки перезапустите терминал/командную строку

**Проверка установки:**
```bash
git --version
```

### 1.2 Настройка Git

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваша-почта@example.com"
```

### 1.3 Создание репозитория локально

Откройте терминал в папке проекта (папка `japan-travel-map`):

```bash
# Перейдите в папку проекта
cd путь/к/папке/japan-travel-map

# Инициализируйте Git репозиторий
git init

# Добавьте все файлы в отслеживание
git add .

# Создайте первый коммит
git commit -m "Initial commit: Japan Travel Map project"
```

### 1.4 Создание репозитория на GitHub

1. Перейдите на https://github.com и зарегистрируйтесь (если ещё нет аккаунта)
2. Нажмите зелёную кнопку "+" в правом верхнем углу → "New repository"
3. Введите название: `japan-travel-map`
4. Оставьте репозиторий публичным (Public)
5. НЕ ставьте галочки на "Add a README file" и других
6. Нажмите "Create repository"

### 1.5 Загрузка проекта на GitHub

GitHub покажет вам команды. Скопируйте и выполните блок:

```bash
git remote add origin https://github.com/ВАШ-НИКНЕЙМ/japan-travel-map.git
git branch -M main
git push -u origin main
```

Готово! Ваш код теперь на GitHub.

---

## Этап 2: Аренда и настройка VPS сервера

### 2.1 Выбор хостинга

**Рекомендуемые варианты:**

| Хостинг | Цена | Ссылка |
|---------|------|--------|
| Timeweb Cloud | ~400₽/мес | https://timeweb.cloud |
| Selectel | ~350₽/мес | https://selectel.ru |
| DigitalOcean | $6/мес | https://www.digitalocean.com |
| Hetzner | ~€5/мес | https://www.hetzner.com |

**Минимальные требования:**
- 1 CPU ядро
- 1-2 GB RAM
- 10-20 GB SSD
- Ubuntu 22.04 LTS

### 2.2 Создание сервера (пример Timeweb)

1. Зарегистрируйтесь на timeweb.cloud
2. Перейдите в "Облачные серверы"
3. Нажмите "Создать сервер"
4. Выберите:
   - Локация: любая (лучше ближе к вам)
   - Операционная система: Ubuntu 22.04 LTS
   - Конфигурация: минимальная (1 CPU, 1-2 GB RAM)
   - Имя сервера: `japan-map-server`
5. Нажмите "Создать"
6. Сохраните IP-адрес сервера и пароль root (придёт на почту)

---

## Этап 3: Подключение к серверу

### 3.1 Подключение через SSH

**Windows (PowerShell или CMD):**
```bash
ssh root@ВАШ_IP_АДРЕС
```

**Mac/Linux (Терминал):**
```bash
ssh root@ВАШ_IP_АДРЕС
```

При первом подключении введите `yes` для подтверждения.

Введите пароль root (при вводе символы не отображаются — это нормально).

**Если не работает:**
- Убедитесь, что сервер запущен в панели управления хостинга
- Проверьте правильность IP-адреса

---

## Этап 4: Настройка сервера

После подключения вы увидите командную строку сервера.

### 4.1 Обновление системы

```bash
apt update && apt upgrade -y
```

### 4.2 Установка необходимых программ

```bash
# Установка Node.js (версия 20 LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Установка Nginx (веб-сервер)
apt install -y nginx

# Установка Git
apt install -y git

# Установка PM2 (менеджер процессов)
npm install -g pm2
```

### 4.3 Проверка установки

```bash
node --version    # Должно показать v20.x.x
npm --version     # Должно показать 10.x.x
git --version     # Должно показать версию Git
nginx -v          # Должно показать версию Nginx
```

---

## Этап 5: Загрузка проекта на сервер

### 5.1 Клонирование репозитория

```bash
# Перейдите в домашнюю директорию
cd ~

# Клонируйте проект (замените URL на ваш)
git clone https://github.com/ВАШ-НИКНЕЙМ/japan-travel-map.git

# Перейдите в папку проекта
cd japan-travel-map
```

### 5.2 Установка зависимостей и сборка

```bash
# Установка всех зависимостей
npm install

# Сборка проекта
npm run build
```

**Если сборка прошла успешно**, вы увидите папку `dist` с собранными файлами.

---

## Этап 6: Настройка Nginx

### 6.1 Создание конфигурации

```bash
# Удалите стандартную конфигурацию
rm /etc/nginx/sites-enabled/default

# Создайте новую конфигурацию
nano /etc/nginx/sites-available/japan-map
```

### 6.2 Вставьте конфигурацию

В открывшемся редакторе вставьте (правый клик вставляет текст):

```nginx
server {
    listen 80;
    server_name _;  # Принимает любой домен
    
    # Корневая папка проекта
    root /root/japan-travel-map/dist;
    index index.html;
    
    # Обработка запросов
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**Сохранение и выход:**
- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить)
- Нажмите `Ctrl+X` (выйти)

### 6.3 Активация конфигурации

```bash
# Создание символической ссылки
ln -s /etc/nginx/sites-available/japan-map /etc/nginx/sites-enabled/

# Проверка конфигурации
nginx -t

# Перезапуск Nginx
systemctl restart nginx

# Настройка автозапуска Nginx
systemctl enable nginx
```

---

## Этап 7: Проверка работы

### 7.1 Откройте браузер

Введите в адресную строку:
```
http://ВАШ_IP_АДРЕС
```

Вы должны увидеть ваш проект Japan Travel Map!

### 7.2 Проверка статуса Nginx

```bash
systemctl status nginx
```

Если всё работает, статус должен быть "active (running)".

---

## Этап 8: Привязка домена (опционально)

### 8.1 Купите домен

Рекомендуемые регистраторы:
- reg.ru
- nic.ru
- namecheap.com

### 8.2 Настройка DNS

В панели управления доменом создайте A-запись:
- Тип: A
- Имя: @ (или www)
- Значение: ВАШ_IP_АДРЕС
- TTL: 3600

### 8.3 Обновление Nginx конфигурации

```bash
nano /etc/nginx/sites-available/japan-map
```

Измените строку:
```nginx
server_name ваш-домен.ru www.ваш-домен.ru;
```

Перезапустите Nginx:
```bash
systemctl restart nginx
```

---

## Этап 9: Настройка HTTPS (SSL) - Опционально

### 9.1 Установка Certbot

```bash
apt install -y certbot python3-certbot-nginx
```

### 9.2 Получение SSL сертификата

```bash
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

Следуйте инструкциям:
- Введите email
- Согласитесь с условиями
- Выберите редирект HTTP → HTTPS (выберите 2)

### 9.3 Автообновление сертификата

```bash
systemctl enable certbot.timer
```

---

## Этап 10: Обновление проекта

Когда вы вносите изменения в проект локально:

### 10.1 Локально (на вашем компьютере)

```bash
# Перейдите в папку проекта
cd japan-travel-map

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push origin main
```

### 10.2 На сервере

```bash
# Перейдите в папку проекта
cd ~/japan-travel-map

# Получите обновления
git pull origin main

# Пересоберите проект
npm install
npm run build

# Перезапустите Nginx
systemctl restart nginx
```

---

## Решение проблем

### Проблема: Порт 80 занят

```bash
# Найдите процесс, занимающий порт 80
lsof -i :80

# Остановите его
kill -9 PID_ПРОЦЕССА

# Перезапустите Nginx
systemctl restart nginx
```

### Проблема: Ошибка 403 Forbidden

```bash
# Проверьте права доступа
chmod -R 755 /root/japan-travel-map/dist
chown -R www-data:www-data /root/japan-travel-map/dist
```

### Проблема: Сборка не проходит

```bash
# Проверьте версию Node.js
node --version  # Должна быть v18 или выше

# Очистите кэш и node_modules
cd ~/japan-travel-map
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: Nginx не запускается

```bash
# Проверьте синтаксис конфигурации
nginx -t

# Посмотрите логи ошибок
tail -f /var/log/nginx/error.log
```

---

## Полезные команды

```bash
# Перезапуск Nginx
systemctl restart nginx

# Просмотр логов Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Проверка свободного места
df -h

# Проверка использования памяти
free -h

# Просмотр запущенных процессов
htop
```

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи: `tail -f /var/log/nginx/error.log`
2. Проверьте статус: `systemctl status nginx`
3. Задайте вопрос в Issues на GitHub

---

**Поздравляем! Ваш проект теперь доступен в интернете!** 🎉

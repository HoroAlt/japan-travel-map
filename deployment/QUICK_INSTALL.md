# 🚀 Быстрая установка

## 📤 Шаг 0: Загрузка проекта на GitHub

Перед развёртыванием на сервере, загрузите проект на GitHub:

### Windows (PowerShell):
```powershell
.\deployment\push-to-github.ps1
```

### Mac/Linux (Bash):
```bash
bash deployment/push-to-github.sh
```

Скрипт попросит:
- Ваш GitHub username
- Название репозитория
- GitHub Personal Access Token (как получить — покажет сам скрипт)

**После выполнения проект будет на GitHub и готов к развёртыванию!**

---

## ⚡ Мгновенная установка на сервер (одной командой)

После аренды VPS сервера, подключитесь к нему по SSH и выполните:

```bash
curl -fsSL https://raw.githubusercontent.com/ВАШ_NICKNAME/japan-travel-map/main/deployment/install.sh | sudo bash
```

**Или если curl не работает:**

```bash
wget -qO- https://raw.githubusercontent.com/ВАШ_NICKNAME/japan-travel-map/main/deployment/install.sh | sudo bash
```

---

## 📋 Пошаговая установка

### Шаг 1: Подключитесь к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
```

### Шаг 2: Загрузите установщик

```bash
# Вариант 1: С помощью curl
curl -O https://raw.githubusercontent.com/ВАШ_NICKNAME/japan-travel-map/main/deployment/install.sh

# Вариант 2: С помощью wget
wget https://raw.githubusercontent.com/ВАШ_NICKNAME/japan-travel-map/main/deployment/install.sh
```

### Шаг 3: Запустите установку

```bash
sudo bash install.sh
```

### Шаг 4: Следуйте инструкциям

Установщик попросит ввести:
- Ваш GitHub username
- Название репозитория (по умолчанию: japan-travel-map)
- Домен (опционально)
- Порт (по умолчанию: 3000)

---

## ✅ Что делает установщик автоматически

1. ✅ Обновляет систему
2. ✅ Устанавливает Node.js 20 LTS
3. ✅ Устанавливает Nginx
4. ✅ Устанавливает Git и PM2
5. ✅ Клонирует ваш репозиторий с GitHub
6. ✅ Устанавливает npm зависимости
7. ✅ Собирает проект (`npm run build`)
8. ✅ Настраивает Nginx
9. ✅ Настраивает права доступа
10. ✅ Предлагает настроить HTTPS (SSL)

---

## 📝 Пример сессии установки

```
root@server:~# curl -O https://raw.githubusercontent.com/johndoe/japan-travel-map/main/deployment/install.sh
root@server:~# sudo bash install.sh

==========================================
  Japan Travel Map - Установщик
==========================================

Введите данные для установки:

Ваш GitHub username: johndoe
Название репозитория [japan-travel-map]: japan-travel-map
Ваш домен (оставьте пустым для IP): mysite.com
Порт для приложения [3000]: 80

[INFO] Начинаю установку...

[INFO] 1/8 Обновление системы...
[OK] Система обновлена

[INFO] 2/8 Установка необходимых пакетов...
[OK] Все пакеты установлены

... и так далее ...

==========================================
  Установка завершена!
==========================================

[OK] Japan Travel Map успешно развёрнут!

📱 Доступ к приложению:
   http://mysite.com
   http://192.168.1.100

📁 Расположение проекта: /var/www/japan-travel-map

📝 Полезные команды:
   Просмотр логов Nginx:  tail -f /var/log/nginx/japan-travel-map-error.log
   Перезапуск Nginx:      systemctl restart nginx
   Проверка статуса:      systemctl status nginx

==========================================
```

---

## 🔧 Ручная установка (если автоматическая не сработала)

Если автоматический установщик по какой-то причине не работает, следуйте подробному гайду в файле [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) или [GITHUB_TOKEN_GUIDE.md](./GITHUB_TOKEN_GUIDE.md)

---

## 🔄 Обновление проекта

После внесения изменений в проект локально:

```bash
# На сервере выполните:
cd /var/www/japan-travel-map
git pull origin main
npm install
npm run build
systemctl restart nginx
```

Или создайте алиас для удобства:

```bash
echo 'alias update-japan-map="cd /var/www/japan-travel-map && git pull && npm install && npm run build && systemctl restart nginx"' >> ~/.bashrc
source ~/.bashrc

# Теперь можно просто:
update-japan-map
```

---

## ❓ Возникли проблемы?

1. Проверьте логи установщика
2. Проверьте логи Nginx: `tail -f /var/log/nginx/error.log`
3. Убедитесь, что репозиторий публичный на GitHub
4. Проверьте правильность введённого GitHub username

---

**Готово к использованию! 🎉**

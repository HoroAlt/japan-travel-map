# 🔑 Как получить GitHub Personal Access Token

GitHub Token нужен для автоматической загрузки проекта на GitHub.

## Быстрая инструкция (2 минуты)

### 1. Перейдите в настройки токенов

Откройте в браузере:
```
https://github.com/settings/tokens
```

Или вручную:
1. Залогиньтесь на GitHub
2. Нажмите на аватарку (правый верхний угол)
3. Выберите **Settings**
4. В меню слева в самом низу выберите **Developer settings**
5. Нажмите **Personal access tokens** → **Tokens (classic)**

### 2. Создайте новый токен

1. Нажмите зелёную кнопку **Generate new token (classic)**
2. Введите название (например: `deploy-script` или `japan-map-token`)
3. Выберите срок действия:
   - **7 days** — если хотите протестировать
   - **30 days** — для временного проекта
   - **No expiration** — для постоянного использования

### 3. Выберите разрешения

Поставьте галочку на:
- ☑ **repo** (полный доступ к репозиториям)

Это даст все необходимые права для создания репозитория и загрузки кода.

### 4. Создайте токен

1. Прокрутите вниз
2. Нажмите зелёную кнопку **Generate token**

### 5. Скопируйте токен

⚠️ **ВАЖНО:** Токен покажется ТОЛЬКО ОДИН РАЗ!

1. Нажмите кнопку копирования (📋) рядом с токеном
2. Вставьте его в безопасное место (например, временно в блокнот)
3. Используйте при запуске скрипта `push-to-github.sh`

---

## ❓ Частые вопросы

### Безопасно ли использовать токен?

Токен — это как пароль. Сохраняйте его в безопасности:
- ✅ Используйте для автоматизации
- ✅ Храните в переменных окружения, если нужно
- ❌ Не сохраняйте в публичных репозиториях
- ❌ Не отправляйте по почте или мессенджерам

### Что если я потерял токен?

Просто создайте новый:
1. Перейдите на https://github.com/settings/tokens
2. Найдите старый токен в списке
3. Нажмите **Delete** рядом с ним
4. Создайте новый по инструкции выше

### Можно ли ограничить права токена?

Для данного проекта минимальные права:
- **repo** — доступ к репозиториям (чтение/запись)

Другие права не нужны.

### Сколько токенов можно создать?

Сколько угодно. Можно создать отдельный токен для каждого проекта.

---

## 🚀 Дальше

После получения токена:

1. Запустите скрипт загрузки:
   ```bash
   bash push-to-github.sh
   ```

2. Вставьте токен когда скрипт попросит

3. Готово! Проект будет на GitHub

---

**Проблемы?** Проверьте:
- Токен скопирован полностью (без пробелов)
- У токена есть права `repo`
- Вы используете правильный GitHub username

# 🗾 Japan Travel Map

Интерактивная карта путешествий по Японии с возможностью отмечать посещённые места.

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка проекта
npm run build

# Предпросмотр собранного проекта
npm run preview
```

## 📦 Развёртывание на сервере

### Вариант 1: Полная автоматизация (рекомендуется)

#### Шаг 1: Загрузка на GitHub

**Windows (PowerShell):**
```powershell
.\deployment\push-to-github.ps1
```

**Mac/Linux (Bash):**
```bash
bash deployment/push-to-github.sh
```

Скрипт автоматически:
- Создаст репозиторий на GitHub
- Загрузит все файлы
- Подготовит всё для развёртывания

#### Шаг 2: Развёртывание на VPS

После загрузки на GitHub, на вашем сервере выполните:

```bash
curl -fsSL https://raw.githubusercontent.com/ВАШ_NICK/japan-travel-map/main/deployment/install.sh | sudo bash
```

### Вариант 2: Ручная настройка

Смотрите подробный гайд в [deployment/DEPLOYMENT_GUIDE.md](./deployment/DEPLOYMENT_GUIDE.md)

## 🛠 Технологии

- **React 19** — библиотека для построения интерфейсов
- **TypeScript** — типизированный JavaScript
- **Vite** — быстрый сборщик
- **D3.js** — визуализация данных и карты
- **TopoJSON** — работа с геоданными

## 📁 Структура проекта

```
japan-travel-map/
├── src/                    # Исходный код
│   ├── components/         # React компоненты
│   ├── data/              # Данные (координаты, города)
│   ├── hooks/             # Custom React hooks
│   ├── styles/            # CSS стили
│   └── types/             # TypeScript типы
├── public/                # Статические файлы
├── dist/                  # Собранный проект (создаётся при build)
├── deployment/            # 📁 Папка с файлами для развёртывания
│   ├── install.sh         # Автоустановщик для сервера
│   ├── push-to-github.sh  # Скрипт загрузки на GitHub (Linux/Mac)
│   ├── push-to-github.ps1 # Скрипт загрузки на GitHub (Windows)
│   ├── DEPLOYMENT_GUIDE.md    # Подробный гайд по развёртыванию
│   ├── QUICK_INSTALL.md       # Краткая инструкция по установке
│   └── GITHUB_TOKEN_GUIDE.md  # Гайд по получению GitHub токена
```

## 🔧 Скрипты

| Скрипт | Описание |
|--------|----------|
| `npm run dev` | Запуск dev-сервера с hot reload |
| `npm run build` | Сборка проекта для продакшена |
| `npm run preview` | Предпросмотр собранного проекта |
| `npm run lint` | Проверка кода ESLint |

## 🚀 Развёртывание

### Требования к серверу

- Ubuntu 22.04 LTS (рекомендуется)
- 1 CPU ядро
- 1-2 GB RAM
- 10 GB SSD

### Автоматическая установка

```bash
# На сервере выполните:
curl -fsSL https://raw.githubusercontent.com/ВАШ_NICK/japan-travel-map/main/deployment/install.sh | sudo bash
```

### Обновление проекта

```bash
cd /var/www/japan-travel-map
git pull origin main
npm install
npm run build
systemctl restart nginx
```

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Создано с любовью к Японии 🇯🇵

---

**Проблемы с развёртыванием?** Смотрите [deployment/DEPLOYMENT_GUIDE.md](./deployment/DEPLOYMENT_GUIDE.md) или создайте Issue на GitHub.
